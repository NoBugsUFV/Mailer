    var checkbox = document.querySelector("input[name=checkbox]");
    checkbox.addEventListener('change', function(){
        if(this.checked){
            //TODO: Colocar o código que:
            
            //Opção 1 - Junta todos os emails no js em uma variável pra depois mandar ela pro PHP.

            //Opção 2 - A cada marcada, faz uma chamada ajax e acumula os destinatários em uma variável em php.
        }
    });